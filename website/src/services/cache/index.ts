/**
 * Cache Service
 * 
 * This module provides caching functionality for the Neo Service Layer.
 * It supports in-memory caching with configurable TTL and automatic cleanup.
 */

// Export the CacheService class
export { CacheService } from './service';

// Export any types
export type { CacheOptions } from './service';
