/**
 * WebSocket Service
 * 
 * This module provides WebSocket client functionality for the Neo Service Layer.
 * It supports real-time data streaming, subscription management, and automatic reconnection.
 */

// Export the websocketService singleton instance
export { websocketService } from './service';

// Export any types
export type { WebSocketMessage } from './service';
